import 'package:flutter/material.dart';

class IngredientTab extends StatelessWidget {
  const IngredientTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Ingredient Tab Content'),
    );
  }
}
